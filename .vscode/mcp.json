{
  "servers": {
    "eslint-todo": {
      "type": "stdio",
      "command": "node",
      "args": [
        "${workspaceFolder}/bin/eslint-todo.mjs",
        "--mcp",
        "--root",
        "${workspaceFolder}"
      ]
    }
  }
}
